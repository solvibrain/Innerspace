import 'package:flutter/material.dart';
import '../presentation/pages/auth/login_page.dart';

// This will be populated with actual routes as they are created
final Map<String, WidgetBuilder> appRoutes = {
  '/': (context) => Container(
        child: Center(child: Text('InnerSpace Home Page Placeholder')),
      ),
};
